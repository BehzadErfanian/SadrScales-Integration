# Tests

Planned test layers:

1. pure unit tests for SDK behavior;
2. SQL contract/schema validation tests;
3. integration tests against a disposable/synthetic database;
4. compatibility tests for supported Sadr Scales contract versions;
5. public-repository security validation.

No customer database or production credentials may be used as committed test fixtures.
