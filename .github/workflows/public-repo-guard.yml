name: Public repository guard

on:
  pull_request:

permissions:
  contents: read

# Protected-branch compatibility check only.
# The full public-boundary validation remains in the final release workflow.
jobs:
  validate-public-boundary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heavy per-change validation disabled by free-tier policy"
