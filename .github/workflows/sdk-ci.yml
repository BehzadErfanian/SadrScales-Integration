name: SDK CI

on:
  pull_request:

permissions:
  contents: read

# Compatibility stub for protected-branch required checks.
# All real build/test work is performed by the release workflow only.
jobs:
  build-test-pack:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipped by free-tier policy"

  sql-integration-test:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipped by free-tier policy"

  net48-package-consumer:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipped by free-tier policy"

  vendor-acceptance:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipped by free-tier policy"
