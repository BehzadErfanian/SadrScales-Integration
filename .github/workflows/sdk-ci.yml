name: SDK CI

on:
  pull_request:

permissions:
  contents: read

# Protected-branch compatibility checks only.
# Real build/test/release work runs from the release workflow, not on each change.
jobs:
  build-test-pack:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heavy CI disabled by free-tier policy"

  sql-integration-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heavy CI disabled by free-tier policy"

  net48-package-consumer:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heavy CI disabled by free-tier policy"

  vendor-acceptance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heavy CI disabled by free-tier policy"
