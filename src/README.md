# Source

The public C# SDK will live here.

Planned namespace/package name: `SadrScales.Integration`.

The API is not frozen yet. Do not add implementation until M1 contract validation is complete and the framework/API decisions are recorded in `docs/DECISIONS.md`.

Planned areas:

- connection/configuration abstraction;
- contract/schema validation;
- item group operations;
- item/PLU operations and batches;
- incremental sales reader;
- optional structured invoice helpers;
- bounded transient SQL retry;
- safe logging with no credentials.
